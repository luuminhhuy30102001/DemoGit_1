@model IEnumerable<WebBaoDoi.Areas.Identity.Data.SampleUser>

@{
    ViewData["Title"] = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";

}

<h2>List Users</h2>

<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Email)
            </th>
            <th>Edit</th>
            <th>Delete</th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model)
        {
            <tr>
                <td>
                    @Html.DisplayFor(modelItem => item.Email)
                </td>
                <td>
                    @Html.ActionLink("✏️", "Edit", new { id = item.Id }, new { @class = "button-shiny warning" })
                </td>
                <td>
                    @Html.ActionLink("🗑️", "Delete", new { id = item.Id }, new { @class = "button-shiny danger" })
                </td>
            </tr>
        }
    </tbody>
</table>
